spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.150.101
      config:
        server-addr: 192.168.150.101
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置
